<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">
<!--        <activity android:name=".SplashActivity"-->
<!--            android:theme="@style/SplashTheme"-->
<!--            android:exported="true">-->
<!--            <intent-filter>-->
<!--                <action android:name="android.intent.action.MAIN" />-->
<!--                <category android:name="android.intent.category.LAUNCHER" />-->
<!--            </intent-filter>-->
<!--        </activity>-->
        <activity android:name=".MakeSchedule3Activity"></activity>
        <activity android:name=".MakeSchedule2Activity"></activity>
        <activity android:name=".MakeSchedule2UiActivity"/>
        <activity android:name=".MakeSchedule4Activity"></activity>
        <activity android:name=".MakeSchedule4CompleteActivity"></activity>
        <activity android:name=".MakeSchedule5Activity"></activity>
        <activity
            android:name=".MakeSchedule5CalendarActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".fragments.ConfirmationActivity" />
        <activity android:name=".MakeSchedule6Activity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>
